# Audio2SRT - 音频转字幕软件依赖

# 核心依赖
faster-whisper>=1.0.0      # 语音识别引擎
customtkinter>=5.2.0       # 现代化GUI框架

# 音频处理
av>=12.0.0                 # PyAV 音频处理库
numpy>=1.24.0              # 数值计算

# 工具库
tqdm>=4.65.0               # 进度条
huggingface-hub>=0.19.0    # 模型下载

# 打包工具（仅构建时需要）
pyinstaller>=6.0.0          # 应用打包
